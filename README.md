# one time each project to start git
git init
git add .
# every time u commit
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/zash1043/my-first-github.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/zash1043/my-first-github.git
git branch -M main
git push -u origin main
